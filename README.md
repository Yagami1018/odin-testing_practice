# webpack_template
My template for webpacks projects, it ships with html-loader, css-loader, style-loader, html-webpack-plugin, webpack-dev-server, jest, @babel/preset-env@^7, and scripts already configured such as webpack.config.js and babel.config.js
